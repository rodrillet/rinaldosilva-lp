"use client"

import React from "react";

// Este é um componente simplificado para resolver erros de importação
export function FontOptimizer() {
  // Na implementação real, otimizaríamos a carga de fontes
  // Aqui, simplesmente retornamos null, já que esse componente não renderiza nada visível
  return null;
} 